# ── Database (PostgreSQL multi-tenant) ────────────────────────────────────────
DATABASE_URL=postgresql://user:password@host:5432/dbname

# ── Encryption (debe coincidir con ENCRYPTION_SECRET del backend NestJS) ──────
ENCRYPTION_SECRET=your-hex-encryption-secret

# ── Chatwoot (global — instancia única) ───────────────────────────────────────
CHATWOOT_URL=http://localhost:4000/api/v1
CHATWOOT_TOKEN=your-chatwoot-api-token

# ── Dify (URL base) ───────────────────────────────────────────────────────────
DIFY_API_URL=http://localhost/v1

# ── Meta API ──────────────────────────────────────────────────────────────────
META_API_VERSION=v19.0

# ── Nella Backend (NestJS — para notificar WebSocket tras upsert_contact) ────
NELLA_BACKEND_URL=http://localhost:3000

# ── Human control timeout (horas antes de que la IA retome automáticamente) ──
HUMAN_CONTROL_TIMEOUT_HOURS=4

# ── Server ────────────────────────────────────────────────────────────────────
PORT=8200
