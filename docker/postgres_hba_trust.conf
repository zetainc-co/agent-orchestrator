# pg_hba.conf - Trust auth for Docker internal network only
# External access (port 15432) is blocked by server firewall
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             172.0.0.0/8             trust
host    all             all             10.0.0.0/8              trust
